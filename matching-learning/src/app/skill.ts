export class Skill {
  // id: number;
  tag: string;
  weight: number;

  /**
   *
   */
  constructor( tag: string, weight: number) {
    // this.id = id;
    this.tag = tag;
    this.weight = weight;
  }
}
