export class Skill {
  show: string;
  tag: string;
  weight: number;

  /**
   *
   */
  constructor( show: string, tag: string, weight: number) {
    this.show = show;
    this.tag = tag;
    this.weight = weight;
  }
}
